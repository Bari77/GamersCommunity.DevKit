{
  "name": "gamerscommunity-devkit",
  "private": true,
  "version": "0.1.0",
  "description": "Outils et contrats de développement pour les équipes jeu GamersCommunity",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
