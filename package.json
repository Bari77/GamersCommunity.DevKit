{
  "name": "gamerscommunity-devkit",
  "private": true,
  "version": "0.2.0",
  "description": "Development tools and contracts for GamersCommunity game teams",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
